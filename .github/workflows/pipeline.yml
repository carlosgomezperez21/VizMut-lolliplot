name: VizMut lolliplot pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  run-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.3.0'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libcurl4-openssl-dev \
            libssl-dev \
            libxml2-dev \
            libfontconfig1-dev \
            libharfbuzz-dev \
            libfribidi-dev \
            libfreetype6-dev \
            libpng-dev \
            libtiff5-dev \
            libjpeg-dev

      - name: Install CRAN packages
        run: |
          install.packages(c(
            'optparse',
            'dplyr',
            'stringr',
            'ggplot2',
            'ggrepel',
            'RColorBrewer',
            'ggnewscale',
            'httr',
            'jsonlite',
            'xml2',
            'R.utils',
            'scales',
            'patchwork'
          ), repos = 'https://cloud.r-project.org')
        shell: Rscript {0}

      - name: Install Bioconductor packages
        run: |
          install.packages('BiocManager',
                           repos = 'https://cloud.r-project.org')
          BiocManager::install(c('rtracklayer', 'GenomicRanges'),
                               ask = FALSE)
        shell: Rscript {0}

      - name: Run pipeline - protein plot
        run: |
          Rscript main.R \
            --variants data/variant_kmt2b_toy.csv \
            --features data/features_kmt2b_toy.csv \
            --plot_type protein \
            --gene_name KMT2B \
            --protein_length 2715 \
            --grid FALSE \
            --output output/lolliplot_protein.png

      - name: Run pipeline - single gene plot
        run: |
          Rscript main.R \
            --variants data/variant_kmt2b_toy.csv \
            --plot_type single_gene \
            --gene_name KMT2B \
            --transcript_id NM_014727.3 \
            --genome hg38 \
            --output output/lolliplot_gene.png

      - name: Run pipeline - multi gene plot
        run: |
          Rscript main.R \
            --variants data/variants_multigene_toy.csv \
            --plot_type multi_gene \
            --gene_list all \
            --genome hg38 \
            --output output/lolliplot_multigene.png

      - name: Upload plots
        uses: actions/upload-artifact@v4
        with:
          name: lolliplots
          path: |
            output/lolliplot_protein.png
            output/lolliplot_gene.png
            output/lolliplot_multigene.png
